# A mall has three parking lots.
# They're all the same size.
# Each has fifteen rows of 50 "slots".
# 1. Set these variable to the appropriate integer literal.
lots = 3
rows = 15
slots = 50

# 2. Write an expression with math operators to calculate total slots.
total_slots = lots*rows*slots

# 3. Confirm that `total_slots` is 2250.
print(total_slots)
